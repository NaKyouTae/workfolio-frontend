/**
 * @deprecated shared/formatters.ts에서 직접 import하세요.
 * 이 파일은 하위 호환성을 위해 re-export만 수행합니다.
 */
export {
  getGenderLabel,
  formatBirthDate,
  formatPhoneNumber,
  formatPublicPeriod,
  getEmploymentTypeLabel,
  formatCareerPeriod,
  calculateTotalCareerPeriod,
  getEducationStatusLabel,
  getActivityTypeLabel,
  getLanguageLabel,
  getLanguageLevelLabel,
} from '../../shared/formatters';
