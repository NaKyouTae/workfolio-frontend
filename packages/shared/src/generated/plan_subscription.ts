// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v5.29.3
// source: plan_subscription.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PlanSubscription } from "./common";

export const protobufPackage = "com.spectrum.workfolio.proto";

export interface PlanSubscriptionListResponse {
  planSubscriptions: PlanSubscription[];
}

export interface PlanSubscriptionGetResponse {
  planSubscription?: PlanSubscription | undefined;
}

export interface PlanSubscriptionCreateRequest {
  planId: string;
  durationMonths: string;
  totalPrice: number;
  monthlyEquivalent: number;
  savingsAmount: number;
  discountRate: number;
  priority: number;
}

export interface PlanSubscriptionUpdateRequest {
  id: string;
  planId: string;
  durationMonths: string;
  totalPrice: number;
  monthlyEquivalent: number;
  savingsAmount: number;
  discountRate: number;
  priority: number;
}

function createBasePlanSubscriptionListResponse(): PlanSubscriptionListResponse {
  return { planSubscriptions: [] };
}

export const PlanSubscriptionListResponse: MessageFns<PlanSubscriptionListResponse> = {
  encode(message: PlanSubscriptionListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.planSubscriptions) {
      PlanSubscription.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlanSubscriptionListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlanSubscriptionListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.planSubscriptions.push(PlanSubscription.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlanSubscriptionListResponse {
    return {
      planSubscriptions: globalThis.Array.isArray(object?.planSubscriptions)
        ? object.planSubscriptions.map((e: any) => PlanSubscription.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PlanSubscriptionListResponse): unknown {
    const obj: any = {};
    if (message.planSubscriptions?.length) {
      obj.planSubscriptions = message.planSubscriptions.map((e) => PlanSubscription.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlanSubscriptionListResponse>, I>>(base?: I): PlanSubscriptionListResponse {
    return PlanSubscriptionListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlanSubscriptionListResponse>, I>>(object: I): PlanSubscriptionListResponse {
    const message = createBasePlanSubscriptionListResponse();
    message.planSubscriptions = object.planSubscriptions?.map((e) => PlanSubscription.fromPartial(e)) || [];
    return message;
  },
};

function createBasePlanSubscriptionGetResponse(): PlanSubscriptionGetResponse {
  return { planSubscription: undefined };
}

export const PlanSubscriptionGetResponse: MessageFns<PlanSubscriptionGetResponse> = {
  encode(message: PlanSubscriptionGetResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.planSubscription !== undefined) {
      PlanSubscription.encode(message.planSubscription, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlanSubscriptionGetResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlanSubscriptionGetResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.planSubscription = PlanSubscription.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlanSubscriptionGetResponse {
    return {
      planSubscription: isSet(object.planSubscription) ? PlanSubscription.fromJSON(object.planSubscription) : undefined,
    };
  },

  toJSON(message: PlanSubscriptionGetResponse): unknown {
    const obj: any = {};
    if (message.planSubscription !== undefined) {
      obj.planSubscription = PlanSubscription.toJSON(message.planSubscription);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlanSubscriptionGetResponse>, I>>(base?: I): PlanSubscriptionGetResponse {
    return PlanSubscriptionGetResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlanSubscriptionGetResponse>, I>>(object: I): PlanSubscriptionGetResponse {
    const message = createBasePlanSubscriptionGetResponse();
    message.planSubscription = (object.planSubscription !== undefined && object.planSubscription !== null)
      ? PlanSubscription.fromPartial(object.planSubscription)
      : undefined;
    return message;
  },
};

function createBasePlanSubscriptionCreateRequest(): PlanSubscriptionCreateRequest {
  return {
    planId: "",
    durationMonths: "",
    totalPrice: 0,
    monthlyEquivalent: 0,
    savingsAmount: 0,
    discountRate: 0,
    priority: 0,
  };
}

export const PlanSubscriptionCreateRequest: MessageFns<PlanSubscriptionCreateRequest> = {
  encode(message: PlanSubscriptionCreateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.planId !== "") {
      writer.uint32(10).string(message.planId);
    }
    if (message.durationMonths !== "") {
      writer.uint32(18).string(message.durationMonths);
    }
    if (message.totalPrice !== 0) {
      writer.uint32(24).uint64(message.totalPrice);
    }
    if (message.monthlyEquivalent !== 0) {
      writer.uint32(32).uint64(message.monthlyEquivalent);
    }
    if (message.savingsAmount !== 0) {
      writer.uint32(40).uint64(message.savingsAmount);
    }
    if (message.discountRate !== 0) {
      writer.uint32(48).uint32(message.discountRate);
    }
    if (message.priority !== 0) {
      writer.uint32(56).uint32(message.priority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlanSubscriptionCreateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlanSubscriptionCreateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.planId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.durationMonths = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalPrice = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.monthlyEquivalent = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.savingsAmount = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.discountRate = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.priority = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlanSubscriptionCreateRequest {
    return {
      planId: isSet(object.planId) ? globalThis.String(object.planId) : "",
      durationMonths: isSet(object.durationMonths) ? globalThis.String(object.durationMonths) : "",
      totalPrice: isSet(object.totalPrice) ? globalThis.Number(object.totalPrice) : 0,
      monthlyEquivalent: isSet(object.monthlyEquivalent) ? globalThis.Number(object.monthlyEquivalent) : 0,
      savingsAmount: isSet(object.savingsAmount) ? globalThis.Number(object.savingsAmount) : 0,
      discountRate: isSet(object.discountRate) ? globalThis.Number(object.discountRate) : 0,
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
    };
  },

  toJSON(message: PlanSubscriptionCreateRequest): unknown {
    const obj: any = {};
    if (message.planId !== "") {
      obj.planId = message.planId;
    }
    if (message.durationMonths !== "") {
      obj.durationMonths = message.durationMonths;
    }
    if (message.totalPrice !== 0) {
      obj.totalPrice = Math.round(message.totalPrice);
    }
    if (message.monthlyEquivalent !== 0) {
      obj.monthlyEquivalent = Math.round(message.monthlyEquivalent);
    }
    if (message.savingsAmount !== 0) {
      obj.savingsAmount = Math.round(message.savingsAmount);
    }
    if (message.discountRate !== 0) {
      obj.discountRate = Math.round(message.discountRate);
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlanSubscriptionCreateRequest>, I>>(base?: I): PlanSubscriptionCreateRequest {
    return PlanSubscriptionCreateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlanSubscriptionCreateRequest>, I>>(
    object: I,
  ): PlanSubscriptionCreateRequest {
    const message = createBasePlanSubscriptionCreateRequest();
    message.planId = object.planId ?? "";
    message.durationMonths = object.durationMonths ?? "";
    message.totalPrice = object.totalPrice ?? 0;
    message.monthlyEquivalent = object.monthlyEquivalent ?? 0;
    message.savingsAmount = object.savingsAmount ?? 0;
    message.discountRate = object.discountRate ?? 0;
    message.priority = object.priority ?? 0;
    return message;
  },
};

function createBasePlanSubscriptionUpdateRequest(): PlanSubscriptionUpdateRequest {
  return {
    id: "",
    planId: "",
    durationMonths: "",
    totalPrice: 0,
    monthlyEquivalent: 0,
    savingsAmount: 0,
    discountRate: 0,
    priority: 0,
  };
}

export const PlanSubscriptionUpdateRequest: MessageFns<PlanSubscriptionUpdateRequest> = {
  encode(message: PlanSubscriptionUpdateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.planId !== "") {
      writer.uint32(18).string(message.planId);
    }
    if (message.durationMonths !== "") {
      writer.uint32(26).string(message.durationMonths);
    }
    if (message.totalPrice !== 0) {
      writer.uint32(32).uint64(message.totalPrice);
    }
    if (message.monthlyEquivalent !== 0) {
      writer.uint32(40).uint64(message.monthlyEquivalent);
    }
    if (message.savingsAmount !== 0) {
      writer.uint32(48).uint64(message.savingsAmount);
    }
    if (message.discountRate !== 0) {
      writer.uint32(56).uint32(message.discountRate);
    }
    if (message.priority !== 0) {
      writer.uint32(64).uint32(message.priority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlanSubscriptionUpdateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlanSubscriptionUpdateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.planId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.durationMonths = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalPrice = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.monthlyEquivalent = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.savingsAmount = longToNumber(reader.uint64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.discountRate = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.priority = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlanSubscriptionUpdateRequest {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      planId: isSet(object.planId) ? globalThis.String(object.planId) : "",
      durationMonths: isSet(object.durationMonths) ? globalThis.String(object.durationMonths) : "",
      totalPrice: isSet(object.totalPrice) ? globalThis.Number(object.totalPrice) : 0,
      monthlyEquivalent: isSet(object.monthlyEquivalent) ? globalThis.Number(object.monthlyEquivalent) : 0,
      savingsAmount: isSet(object.savingsAmount) ? globalThis.Number(object.savingsAmount) : 0,
      discountRate: isSet(object.discountRate) ? globalThis.Number(object.discountRate) : 0,
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
    };
  },

  toJSON(message: PlanSubscriptionUpdateRequest): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.planId !== "") {
      obj.planId = message.planId;
    }
    if (message.durationMonths !== "") {
      obj.durationMonths = message.durationMonths;
    }
    if (message.totalPrice !== 0) {
      obj.totalPrice = Math.round(message.totalPrice);
    }
    if (message.monthlyEquivalent !== 0) {
      obj.monthlyEquivalent = Math.round(message.monthlyEquivalent);
    }
    if (message.savingsAmount !== 0) {
      obj.savingsAmount = Math.round(message.savingsAmount);
    }
    if (message.discountRate !== 0) {
      obj.discountRate = Math.round(message.discountRate);
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlanSubscriptionUpdateRequest>, I>>(base?: I): PlanSubscriptionUpdateRequest {
    return PlanSubscriptionUpdateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlanSubscriptionUpdateRequest>, I>>(
    object: I,
  ): PlanSubscriptionUpdateRequest {
    const message = createBasePlanSubscriptionUpdateRequest();
    message.id = object.id ?? "";
    message.planId = object.planId ?? "";
    message.durationMonths = object.durationMonths ?? "";
    message.totalPrice = object.totalPrice ?? 0;
    message.monthlyEquivalent = object.monthlyEquivalent ?? 0;
    message.savingsAmount = object.savingsAmount ?? 0;
    message.discountRate = object.discountRate ?? 0;
    message.priority = object.priority ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
