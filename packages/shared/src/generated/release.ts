// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v5.29.3
// source: release.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Notice, Plan_PlanType, plan_PlanTypeFromJSON, plan_PlanTypeToJSON } from "./common";

export const protobufPackage = "com.spectrum.workfolio.proto";

export interface ReleaseNoticeListResponse {
  notices: Notice[];
}

export interface ReleasePlanSubscription {
  id: string;
  durationMonths: string;
  totalPrice: number;
  monthlyEquivalent: number;
  savingsAmount: number;
  discountRate: number;
  priority: number;
  createdAt: number;
  updatedAt: number;
}

export interface ReleasePlanDetail {
  id: string;
  name: string;
  type: Plan_PlanType;
  price: number;
  currency: string;
  priority: number;
  description?: string | undefined;
  planSubscriptions: ReleasePlanSubscription[];
  createdAt: number;
  updatedAt: number;
}

export interface ReleasePlanListResponse {
  plans: ReleasePlanDetail[];
}

function createBaseReleaseNoticeListResponse(): ReleaseNoticeListResponse {
  return { notices: [] };
}

export const ReleaseNoticeListResponse: MessageFns<ReleaseNoticeListResponse> = {
  encode(message: ReleaseNoticeListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.notices) {
      Notice.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseNoticeListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseNoticeListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.notices.push(Notice.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleaseNoticeListResponse {
    return {
      notices: globalThis.Array.isArray(object?.notices) ? object.notices.map((e: any) => Notice.fromJSON(e)) : [],
    };
  },

  toJSON(message: ReleaseNoticeListResponse): unknown {
    const obj: any = {};
    if (message.notices?.length) {
      obj.notices = message.notices.map((e) => Notice.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleaseNoticeListResponse>, I>>(base?: I): ReleaseNoticeListResponse {
    return ReleaseNoticeListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleaseNoticeListResponse>, I>>(object: I): ReleaseNoticeListResponse {
    const message = createBaseReleaseNoticeListResponse();
    message.notices = object.notices?.map((e) => Notice.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReleasePlanSubscription(): ReleasePlanSubscription {
  return {
    id: "",
    durationMonths: "",
    totalPrice: 0,
    monthlyEquivalent: 0,
    savingsAmount: 0,
    discountRate: 0,
    priority: 0,
    createdAt: 0,
    updatedAt: 0,
  };
}

export const ReleasePlanSubscription: MessageFns<ReleasePlanSubscription> = {
  encode(message: ReleasePlanSubscription, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.durationMonths !== "") {
      writer.uint32(18).string(message.durationMonths);
    }
    if (message.totalPrice !== 0) {
      writer.uint32(24).uint64(message.totalPrice);
    }
    if (message.monthlyEquivalent !== 0) {
      writer.uint32(32).uint64(message.monthlyEquivalent);
    }
    if (message.savingsAmount !== 0) {
      writer.uint32(40).uint64(message.savingsAmount);
    }
    if (message.discountRate !== 0) {
      writer.uint32(48).uint32(message.discountRate);
    }
    if (message.priority !== 0) {
      writer.uint32(56).uint32(message.priority);
    }
    if (message.createdAt !== 0) {
      writer.uint32(784).uint64(message.createdAt);
    }
    if (message.updatedAt !== 0) {
      writer.uint32(792).uint64(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleasePlanSubscription {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleasePlanSubscription();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.durationMonths = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalPrice = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.monthlyEquivalent = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.savingsAmount = longToNumber(reader.uint64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.discountRate = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.priority = reader.uint32();
          continue;
        }
        case 98: {
          if (tag !== 784) {
            break;
          }

          message.createdAt = longToNumber(reader.uint64());
          continue;
        }
        case 99: {
          if (tag !== 792) {
            break;
          }

          message.updatedAt = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleasePlanSubscription {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      durationMonths: isSet(object.durationMonths) ? globalThis.String(object.durationMonths) : "",
      totalPrice: isSet(object.totalPrice) ? globalThis.Number(object.totalPrice) : 0,
      monthlyEquivalent: isSet(object.monthlyEquivalent) ? globalThis.Number(object.monthlyEquivalent) : 0,
      savingsAmount: isSet(object.savingsAmount) ? globalThis.Number(object.savingsAmount) : 0,
      discountRate: isSet(object.discountRate) ? globalThis.Number(object.discountRate) : 0,
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
      updatedAt: isSet(object.updatedAt) ? globalThis.Number(object.updatedAt) : 0,
    };
  },

  toJSON(message: ReleasePlanSubscription): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.durationMonths !== "") {
      obj.durationMonths = message.durationMonths;
    }
    if (message.totalPrice !== 0) {
      obj.totalPrice = Math.round(message.totalPrice);
    }
    if (message.monthlyEquivalent !== 0) {
      obj.monthlyEquivalent = Math.round(message.monthlyEquivalent);
    }
    if (message.savingsAmount !== 0) {
      obj.savingsAmount = Math.round(message.savingsAmount);
    }
    if (message.discountRate !== 0) {
      obj.discountRate = Math.round(message.discountRate);
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.updatedAt !== 0) {
      obj.updatedAt = Math.round(message.updatedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleasePlanSubscription>, I>>(base?: I): ReleasePlanSubscription {
    return ReleasePlanSubscription.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleasePlanSubscription>, I>>(object: I): ReleasePlanSubscription {
    const message = createBaseReleasePlanSubscription();
    message.id = object.id ?? "";
    message.durationMonths = object.durationMonths ?? "";
    message.totalPrice = object.totalPrice ?? 0;
    message.monthlyEquivalent = object.monthlyEquivalent ?? 0;
    message.savingsAmount = object.savingsAmount ?? 0;
    message.discountRate = object.discountRate ?? 0;
    message.priority = object.priority ?? 0;
    message.createdAt = object.createdAt ?? 0;
    message.updatedAt = object.updatedAt ?? 0;
    return message;
  },
};

function createBaseReleasePlanDetail(): ReleasePlanDetail {
  return {
    id: "",
    name: "",
    type: 0,
    price: 0,
    currency: "",
    priority: 0,
    description: undefined,
    planSubscriptions: [],
    createdAt: 0,
    updatedAt: 0,
  };
}

export const ReleasePlanDetail: MessageFns<ReleasePlanDetail> = {
  encode(message: ReleasePlanDetail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.price !== 0) {
      writer.uint32(32).uint64(message.price);
    }
    if (message.currency !== "") {
      writer.uint32(42).string(message.currency);
    }
    if (message.priority !== 0) {
      writer.uint32(48).int32(message.priority);
    }
    if (message.description !== undefined) {
      writer.uint32(58).string(message.description);
    }
    for (const v of message.planSubscriptions) {
      ReleasePlanSubscription.encode(v!, writer.uint32(402).fork()).join();
    }
    if (message.createdAt !== 0) {
      writer.uint32(784).uint64(message.createdAt);
    }
    if (message.updatedAt !== 0) {
      writer.uint32(792).uint64(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleasePlanDetail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleasePlanDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.price = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.currency = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.priority = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 50: {
          if (tag !== 402) {
            break;
          }

          message.planSubscriptions.push(ReleasePlanSubscription.decode(reader, reader.uint32()));
          continue;
        }
        case 98: {
          if (tag !== 784) {
            break;
          }

          message.createdAt = longToNumber(reader.uint64());
          continue;
        }
        case 99: {
          if (tag !== 792) {
            break;
          }

          message.updatedAt = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleasePlanDetail {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? plan_PlanTypeFromJSON(object.type) : 0,
      price: isSet(object.price) ? globalThis.Number(object.price) : 0,
      currency: isSet(object.currency) ? globalThis.String(object.currency) : "",
      priority: isSet(object.priority) ? globalThis.Number(object.priority) : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : undefined,
      planSubscriptions: globalThis.Array.isArray(object?.planSubscriptions)
        ? object.planSubscriptions.map((e: any) => ReleasePlanSubscription.fromJSON(e))
        : [],
      createdAt: isSet(object.createdAt) ? globalThis.Number(object.createdAt) : 0,
      updatedAt: isSet(object.updatedAt) ? globalThis.Number(object.updatedAt) : 0,
    };
  },

  toJSON(message: ReleasePlanDetail): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = plan_PlanTypeToJSON(message.type);
    }
    if (message.price !== 0) {
      obj.price = Math.round(message.price);
    }
    if (message.currency !== "") {
      obj.currency = message.currency;
    }
    if (message.priority !== 0) {
      obj.priority = Math.round(message.priority);
    }
    if (message.description !== undefined) {
      obj.description = message.description;
    }
    if (message.planSubscriptions?.length) {
      obj.planSubscriptions = message.planSubscriptions.map((e) => ReleasePlanSubscription.toJSON(e));
    }
    if (message.createdAt !== 0) {
      obj.createdAt = Math.round(message.createdAt);
    }
    if (message.updatedAt !== 0) {
      obj.updatedAt = Math.round(message.updatedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleasePlanDetail>, I>>(base?: I): ReleasePlanDetail {
    return ReleasePlanDetail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleasePlanDetail>, I>>(object: I): ReleasePlanDetail {
    const message = createBaseReleasePlanDetail();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.price = object.price ?? 0;
    message.currency = object.currency ?? "";
    message.priority = object.priority ?? 0;
    message.description = object.description ?? undefined;
    message.planSubscriptions = object.planSubscriptions?.map((e) => ReleasePlanSubscription.fromPartial(e)) || [];
    message.createdAt = object.createdAt ?? 0;
    message.updatedAt = object.updatedAt ?? 0;
    return message;
  },
};

function createBaseReleasePlanListResponse(): ReleasePlanListResponse {
  return { plans: [] };
}

export const ReleasePlanListResponse: MessageFns<ReleasePlanListResponse> = {
  encode(message: ReleasePlanListResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.plans) {
      ReleasePlanDetail.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleasePlanListResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleasePlanListResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.plans.push(ReleasePlanDetail.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleasePlanListResponse {
    return {
      plans: globalThis.Array.isArray(object?.plans) ? object.plans.map((e: any) => ReleasePlanDetail.fromJSON(e)) : [],
    };
  },

  toJSON(message: ReleasePlanListResponse): unknown {
    const obj: any = {};
    if (message.plans?.length) {
      obj.plans = message.plans.map((e) => ReleasePlanDetail.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReleasePlanListResponse>, I>>(base?: I): ReleasePlanListResponse {
    return ReleasePlanListResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReleasePlanListResponse>, I>>(object: I): ReleasePlanListResponse {
    const message = createBaseReleasePlanListResponse();
    message.plans = object.plans?.map((e) => ReleasePlanDetail.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
